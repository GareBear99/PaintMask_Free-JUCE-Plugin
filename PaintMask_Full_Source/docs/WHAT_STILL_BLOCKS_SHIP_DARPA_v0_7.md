# What Still Blocks Ship — PaintMask DARPA v0.7

- Local JUCE/Xcode compile pass still required
- AU/VST3 validation still required
- layer-specific UI and persistence still partial
- preview synth is intentionally lightweight, not a flagship engine
- audio-to-paint import exists only as a code path, not a finished file workflow
- subscription backend and notarized release path are still external tasks
